import { defineFunction } from '@aws-amplify/backend';
/**
 * Define and configure your post confimation resource
 */

export const postConfirmation = defineFunction({
  name: 'post-confirmation',
});
